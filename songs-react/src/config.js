const CONFIG = {
  autoScrollSpeed: 112, // pixels per second (was 150, reduced ~25%)
  pixelsPerSecond: 112,
  fadeOutSeconds: 1.2,
  scrollResumeDelay: 150 // ms before resuming auto-scroll after manual scroll
}

export default CONFIG
